package torch.pandas.component

import torch.pandas.DataFrame

object ArrowCompat {

  
//  def readArrow(file: String): DataFrame[AnyRef] = Serialization.readArrow(file)
//  
//  def writeArrow(df: DataFrame[AnyRef], file: String): Unit = Serialization.writeArrow(df, file)
}
