package torch.pandas.component

import torch.pandas.DataFrame
object AvroCompat {

//  def readAvro(file: String): DataFrame[AnyRef] = Serialization.readAvro(file)
//
//  def writeAvro(df: DataFrame[AnyRef], file: String): Unit = Serialization.writeAvro(df, file)
}
