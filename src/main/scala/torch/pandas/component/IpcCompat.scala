package torch.pandas.component

import torch.pandas.DataFrame
object IpcCompat {

//  def readIpc(file: String): DataFrame[AnyRef] = Serialization.readIpc(file)
//
//  def writeIpc(df: DataFrame[AnyRef], file: String): Unit = Serialization.writeIpc(df, file)
}
