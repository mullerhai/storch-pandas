package torch.pandas.component

import torch.pandas.DataFrame
object ParquetCompat {

//  def readParquet(file: String): DataFrame[AnyRef] = Serialization.readParquet(file)
//
//  def writeParquet(df: DataFrame[AnyRef], file: String): Unit = Serialization.writeParquet(df, file)

}
